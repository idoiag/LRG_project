TO BE DONE

Program: 
1. Deal with several transcripts:  IGP
2. Create bed file: IGP
3. Compare builds: VF
4. Adding further tests: 
    - strand test: IGP 
    - xml format: VF
    - build test: VF

Documentation:
1. COPYRIGHT (IGP + VF):
  - creation and update (University): IGP
  - optmisation (checking NHS policy) :VF

2. README (IGP + VF)
 1. Background: VF
 2. Structure: IGP
 3. Features description:
    0. initial tests: IGP + VF (as added by author)
    1. get_structure(data) : VF
    2. get_background(root) : VF
    3. get_build_info(up_anno): VF
    4. get_up_anno(data) : VF
    5. get_exon_data(data) : IGP
    6. output2file(): IGP
    7. disclaimer(): IGP 
    8. final_tests(): IGP + VF (as added by author)
 
 4. Disclaimer: IGP
 
